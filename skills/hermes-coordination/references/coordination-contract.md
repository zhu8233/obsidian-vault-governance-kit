# Coordination Contract

Hermes should intervene when:

- two notes claim the same canonical role
- the queue contains a candidate that would overwrite stable knowledge
- duplicate topics should be merged
- archive actions affect canonical lineage

Hermes should avoid intervening when the issue is only a local intake cleanup that another agent can handle safely.
