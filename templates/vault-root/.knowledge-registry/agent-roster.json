{
  "version": "1.0",
  "agents": [
    {
      "agent_id": "human",
      "role": "owner",
      "allowed_layers": ["system", "intake", "curation", "canonical", "archive"],
      "forbidden_layers": [],
      "default_operations": ["capture", "normalize", "curate", "propose", "promote", "merge", "audit", "archive"],
      "requires_registry_write": false,
      "conflict_policy": "human-wins"
    },
    {
      "agent_id": "hermes",
      "role": "coordinator",
      "allowed_layers": ["system", "intake", "curation", "canonical", "archive"],
      "forbidden_layers": [],
      "default_operations": ["curate", "propose", "promote", "merge", "audit", "archive"],
      "requires_registry_write": true,
      "conflict_policy": "proposal-or-merge"
    }
  ]
}
