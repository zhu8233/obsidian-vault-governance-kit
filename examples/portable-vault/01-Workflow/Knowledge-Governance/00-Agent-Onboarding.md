# Agent Onboarding

In this example vault, any agent should:

1. read `RULES.md`
2. inspect `.knowledge-registry/vault-registry.json`
3. determine `topic_id` and layer
4. avoid direct canonical edits unless explicitly authorized
